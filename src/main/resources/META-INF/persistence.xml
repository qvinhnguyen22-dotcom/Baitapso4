<?xml version="1.0" encoding="UTF-8"?>
<persistence version="3.0"
    xmlns="https://jakarta.ee/xml/ns/persistence"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">

    <persistence-unit name="jpa-crud" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>vn.iotstar.entity.User</class>
        <class>vn.iotstar.entity.Category</class>
        <class>vn.iotstar.entity.Product</class>
        <class>vn.iotstar.entity.Video</class>

        <properties>
            <!-- JDBC Driver -->
            <property name="jakarta.persistence.jdbc.driver"
                      value="com.microsoft.sqlserver.jdbc.SQLServerDriver"/>
            
            <!-- Chuỗi URL kết nối SQLEXPRESS chuẩn -->
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:sqlserver://localhost;instanceName=SQLEXPRESS;databaseName=JPA_DB;encrypt=false;trustServerCertificate=true"/>
            
            <!-- Tài khoản sa -->
            <property name="jakarta.persistence.jdbc.user" value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value="123456"/>

            <!-- Cấu hình Hibernate Dialect & Schema -->
            <property name="hibernate.dialect"
                      value="org.hibernate.dialect.SQLServerDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>